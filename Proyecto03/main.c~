#include <stdlib.h>
#include <stdio.h>
#include <string.h>
#include "configReader.h"


int carMean;
int carCounter;
  



int main(){
configReader(&carMean, &carCounter);
printf("carMean = %d carCounter = %d in main.c\n", *carMean, *carCounter);
return 0;

}
